{
  "_meta": {
    "description": "Per-field documentation for POST /models/load `options` object. Sourced from sd.cpp (include/stable-diffusion.h + .cpp consumers); keep these in sync with the C++ ModelLoadParams struct.",
    "fields": [
      "label",
      "description",
      "type",
      "default",
      "values",
      "recommended"
    ]
  },
  "options": {
    "n_threads": {
      "label": "CPU Threads",
      "description": "Number of CPU threads for model operations (loading, mmap fault-in, conditioner forward passes when keep_clip_on_cpu is set). Set to -1 for automatic detection (uses std::thread::hardware_concurrency()).",
      "type": "number",
      "default": -1,
      "recommended": "Leave at -1 (auto). Lower it only if you're sharing the box with other CPU-heavy workloads."
    },
    "flash_attn": {
      "label": "Flash Attention (CLIP / T5)",
      "description": "Enable Flash Attention for the conditioner (CLIP / T5 / LLM). Faster and lower-VRAM attention. CUDA-only.",
      "type": "boolean",
      "default": true,
      "recommended": "Keep on. Disable only if you suspect numerical issues in the conditioner."
    },
    "diffusion_flash_attn": {
      "label": "Flash Attention (Diffusion)",
      "description": "Enable Flash Attention specifically for the diffusion model (UNet / DiT / Flux / MMDiT). sd.cpp keeps this independent from `flash_attn` because CUDA support and numerical stability used to differ between the two.",
      "type": "boolean",
      "default": false,
      "recommended": "Try on for SDXL / Flux / SD3 / Z-Image. Bench against off — usually faster, occasionally produces different outputs at the per-pixel level."
    },
    "enable_mmap": {
      "label": "Memory-mapped Loading",
      "description": "mmap() the model file instead of read()-ing it into anonymous memory. Much faster initial load (no full file read) and the kernel can evict pages under memory pressure.",
      "type": "boolean",
      "default": true,
      "recommended": "Keep on. Disable only on filesystems that don't handle large mmaps well (some network mounts)."
    },
    "vae_conv_direct": {
      "label": "VAE Direct Convolution",
      "description": "Route the VAE's Conv2D through ggml_conv2d_direct instead of the default im2col+gemm path (sd.cpp PR #744). The direct kernel uses dramatically less compute-buffer memory and is faster on Vulkan / OpenCL / CPU — PR benchmarks show ~2-4× faster VAE decode and lets SDXL@1024 fit in <3 GB instead of OOMing. Backend-dependent: on CUDA the direct path is currently slower for the VAE.",
      "type": "boolean",
      "default": false,
      "recommended": "Vulkan / OpenCL / CPU: enable — large VRAM and speed win for the VAE step, especially at higher resolutions. CUDA: leave OFF (measured: slower VAE decode). Verified against sd.cpp PR #744 + a local CUDA test."
    },
    "diffusion_conv_direct": {
      "label": "Diffusion Direct Convolution",
      "description": "Same Conv2D direct kernel (sd.cpp PR #744) applied to the diffusion model's Conv2D layers. Affects every sampler step, so any per-step delta scales by --steps. With recent ggml updates the Vulkan slowdown is mostly gone but still slightly slower; CUDA shows the same pattern as the VAE flag.",
      "type": "boolean",
      "default": false,
      "recommended": "Vulkan / OpenCL / CPU: try it — usually neutral or a small win and lowers compute-buffer memory. CUDA: leave OFF. Flux/DiT models don't use Conv2D in the transformer, so this flag is a no-op for them (only matters for UNet-style architectures + ESRGAN/ControlNet)."
    },
    "tae_preview_only": {
      "label": "TAESD Preview Only",
      "description": "Use TAESD (Tiny AutoEncoder) only for live preview frames during sampling — the final image still goes through the full VAE. TAESD weights are ~10 MiB so the cost is trivial.",
      "type": "boolean",
      "default": false,
      "recommended": "Enable when you're loading a TAESD model just for previews and want full-quality finals."
    },
    "weight_type": {
      "label": "Weight Type",
      "description": "Override the precision used for model weights at runtime. Lower precision uses less VRAM but may affect quality. Empty string = use the model's native precision (read from the safetensors / GGUF header).",
      "type": "select",
      "default": "",
      "values": {
        "": "Auto (use the file's native precision)",
        "f32": "32-bit float — highest quality, most VRAM",
        "f16": "16-bit float — common default",
        "bf16": "Brain float 16 — same memory as f16 with wider exponent range",
        "q8_0": "8-bit quantized — ~50% memory of f16, near-lossless",
        "q5_0": "5-bit quantized — smaller, slight quality loss",
        "q4_0": "4-bit quantized — smallest, noticeable quality loss",
        "q4_k": "4-bit K-quants — smaller than q4_0 with better quality",
        "q5_k": "5-bit K-quants — middle ground",
        "q6_k": "6-bit K-quants — best quality of K-quants"
      },
      "recommended": "Leave on Auto. Override only when the source file is f32 / f16 and you want to force a smaller runtime size."
    },
    "tensor_type_rules": {
      "label": "Per-tensor Weight Rules",
      "description": "sd.cpp regex rules for per-tensor precision overrides, e.g. `^vae\\.=f16` to force the VAE to fp16 even when the rest of the model is q4. Multiple rules are comma-separated.",
      "type": "string",
      "default": "",
      "recommended": "Empty unless you know the tensor naming convention. See sd.cpp's `--tensor-type-rules` documentation."
    },
    "rng_type": {
      "label": "RNG Type",
      "description": "Random number generator backend used to draw initial noise. `cuda` produces output that matches comfyui / diffusers on NVIDIA. `cpu` is deterministic across hardware but slower at start-of-generation. `std_default` is C++ std::mt19937.",
      "type": "select",
      "default": "cuda",
      "values": {
        "cuda": "CUDA RNG — matches comfyui / diffusers on NVIDIA",
        "cpu": "CPU RNG — deterministic across hardware, slightly slower",
        "std_default": "C++ std::mt19937 — least common"
      },
      "recommended": "Keep `cuda` to match ecosystem outputs. Use `cpu` if you need reproducibility across different GPUs."
    },
    "sampler_rng_type": {
      "label": "Sampler RNG Type",
      "description": "RNG used by stochastic samplers (DPM++ ancestral, etc.). Empty = same as `rng_type`. Separating the two lets you have CUDA-RNG initial noise but CPU-RNG sampler steps for partial reproducibility.",
      "type": "string",
      "default": "",
      "recommended": "Empty (matches rng_type)."
    },
    "prediction": {
      "label": "Prediction Type",
      "description": "Override sd.cpp's auto-detection of the model's prediction objective. Empty = auto-detect from the safetensors header / model class.",
      "type": "select",
      "default": "",
      "values": {
        "": "Auto (detect from model)",
        "eps": "Epsilon (noise) — SD1.x, SDXL",
        "v": "v-prediction — SD2.1-v, some SDXL variants",
        "edm_v": "EDM v-prediction",
        "sd3_flow": "SD3 flow matching",
        "flux_flow": "Flux flow matching",
        "flux2_flow": "Flux2 flow matching"
      },
      "recommended": "Leave on Auto. Override only when sd.cpp misdetects (rare; happens with custom-trained models that lack the usual metadata)."
    },
    "lora_apply_mode": {
      "label": "LoRA Apply Mode",
      "description": "When LoRAs are merged into the base model. `auto` lets sd.cpp decide based on the model architecture. `immediately` merges at load time (faster generation, slower load, can't change LoRAs without reload). `at_runtime` keeps LoRAs separate and applies them per generation (faster load, slightly slower generation, can swap LoRAs without reloading the model).",
      "type": "select",
      "default": "auto",
      "values": {
        "auto": "Auto — sd.cpp picks per architecture",
        "immediately": "Immediately — merge into weights at load (fast generation, immutable)",
        "at_runtime": "At runtime — apply per-generation (mutable, ~5% slower per step)"
      },
      "recommended": "`auto` for the unified-streaming + offload-to-cpu path — sd.cpp's planner needs to know whether base weights will be mutated before deciding chunk-K residency. Forcing `at_runtime` on a streaming workload makes every chunk re-apply the LoRA delta after the base tensor streams in, which costs extra per-step time. Use `at_runtime` explicitly only when you need to hot-swap LoRAs without reloading the model and you're not streaming."
    },
    "force_sdxl_vae_conv_scale": {
      "label": "Force SDXL VAE Conv Scale",
      "description": "Apply SDXL-specific VAE convolution scaling unconditionally (sd.cpp normally autodetects). Internal compatibility flag for SDXL VAEs that lack the scale metadata.",
      "type": "boolean",
      "default": false,
      "recommended": "Off. Enable only if you have a stripped SDXL VAE that's producing washed-out outputs."
    },
    "max_vram": {
      "label": "Max VRAM (GiB)",
      "description": "GiB budget for graph-cut segmented param offload (PR #1476). When set, sd.cpp partitions the diffusion graph so peak weight residency stays under this budget. Lives on `sd_ctx_params_t` and is independent of `offload_mode` — they can be combined. 0 = disabled.",
      "type": "number",
      "default": 0.0,
      "recommended": "0 (disabled) by default. Set to slightly less than your total VRAM (e.g. 10 on a 12 GiB card) when the model still OOMs at the smallest sample step. Combines well with `offload_mode = layer_streaming`."
    },
    "stream_layers": {
      "label": "Stream Layers (unified-streaming)",
      "description": "Engage sd.cpp's residency+async-prefetch streaming on top of the `max_vram` budget. The planner picks the resident/streamed split automatically and overlaps next-segment H2D with current-segment compute, so per-step overhead is much lower than the legacy per-layer streaming. Only exposed when restapi is built with `-DSD_UNIFIED_STREAMING=ON` against the fork's `feature/unified-streaming` branch. Has no effect when `max_vram == 0`.",
      "type": "boolean",
      "default": false,
      "recommended": "On with `max_vram` set to ~80% of your free VRAM. Replaces the legacy `offload_mode=layer_streaming` workflow — don't combine the two (they target different fork branches)."
    },
    "vae_format": {
      "label": "VAE Format Override",
      "description": "Tells sd.cpp which VAE family the loaded VAE belongs to. `auto` (default) lets sd.cpp detect from the weights' metadata. Explicit values are required for the PiD architecture (where standalone VAE files don't carry the metadata sd.cpp needs to distinguish Flux / SD3 / Flux.2 latents) and useful when a third-party VAE strips that metadata.",
      "type": "select",
      "default": "auto",
      "values": {
        "auto": "Auto — detect from VAE weights (default)",
        "flux": "Flux — Flux / Z-Image / Ideogram / Lens / Ernie latents",
        "sd3": "SD3 — SD3.x latents",
        "flux2": "Flux.2 — Flux.2 latents"
      },
      "recommended": "Leave on `auto`. Set explicitly only for PiD (per its doc) or when you have a stripped VAE file."
    },
    "backend": {
      "label": "Compute backend",
      "type": "text",
      "default": "",
      "description": "Compute backend override (replaces keep_clip_on_cpu / keep_vae_on_cpu / keep_controlnet_on_cpu). Empty = sd.cpp picks; supports per-component placement e.g. 'te=cpu,vae=cpu,diffusion=cuda0'.",
      "recommended": "Empty for standard runs. 'te=cpu,vae=cpu' when the diffusion model is large enough to need VRAM headroom."
    },
    "params_backend": {
      "label": "Parameter storage backend",
      "type": "text",
      "default": "",
      "description": "Where param tensors live (replaces offload_to_cpu). '*=cpu' keeps all weights in host RAM and streams into VRAM on demand.",
      "recommended": "Empty by default. '*=cpu' with stream_layers=true + max_vram > 0 for very large models on limited VRAM."
    },
    "model_args": {
      "label": "Model args",
      "type": "text",
      "default": "",
      "description": "Model-specific comma-separated key=value list (replaces chroma_use_dit_mask, chroma_use_t5_mask, chroma_t5_mask_pad, qwen_image_zero_cond_t, etc.). e.g. 'chroma_use_dit_mask=true,chroma_t5_mask_pad=1'.",
      "recommended": "Empty unless a specific architecture doc says otherwise."
    },
    "rpc_servers": {
      "label": "RPC servers",
      "type": "text",
      "default": "",
      "description": "Comma-separated host:port pairs for distributed-backend (rpc) workers.",
      "recommended": "Empty for single-machine setups."
    },
    "eager_load": {
      "label": "Eager load",
      "type": "switch",
      "default": true,
      "description": "Pre-load all params into the params backend at load-time instead of lazily on first use (leejet PR #1687). Trades load time for first-generation latency.",
      "recommended": "true for long-lived servers (default). false for one-shot CLI use."
    }
  },
  "categories": {
    "performance": {
      "label": "Performance",
      "options": [
        "flash_attn",
        "diffusion_flash_attn",
        "enable_mmap",
        "n_threads",
        "vae_conv_direct",
        "diffusion_conv_direct"
      ]
    },
    "memory": {
      "label": "Memory Management",
      "options": [
        "keep_clip_on_cpu",
        "keep_vae_on_cpu",
        "keep_controlnet_on_cpu",
        "offload_to_cpu",
        "free_params_immediately",
        "max_vram",
        "weight_type",
        "tensor_type_rules"
      ]
    },
    "vae": {
      "label": "VAE Settings",
      "options": [
        "vae_decode_only",
        "vae_tiling",
        "vae_tile_size_x",
        "vae_tile_size_y",
        "vae_tile_overlap",
        "tae_preview_only",
        "force_sdxl_vae_conv_scale"
      ]
    },
    "offloading": {
      "label": "Dynamic VRAM Offloading",
      "options": [
        "offload_mode",
        "vram_estimation",
        "offload_cond_stage",
        "offload_diffusion",
        "reload_cond_stage",
        "reload_diffusion",
        "log_offload_events",
        "min_offload_size_mb",
        "target_free_vram_mb"
      ]
    },
    "layer_streaming": {
      "label": "Layer Streaming Tuning",
      "options": [
        "streaming_prefetch_layers",
        "streaming_keep_layers_behind",
        "streaming_min_free_vram_mb"
      ]
    },
    "rng_and_prediction": {
      "label": "RNG & Prediction",
      "options": [
        "rng_type",
        "sampler_rng_type",
        "prediction",
        "lora_apply_mode",
        "flow_shift"
      ]
    },
    "chroma": {
      "label": "Chroma-specific",
      "options": [
        "chroma_use_dit_mask",
        "chroma_use_t5_mask",
        "chroma_t5_mask_pad"
      ]
    }
  }
}